# LLaMA-Factory SFT config placeholder for Stage 0.
# Assumes `llamafactory-cli` is installed separately.
stage: sft
do_train: true
model_name_or_path: Qwen/Qwen3-8B
finetuning_type: lora
template: qwen
dataset: ecom_sft_seed
dataset_dir: data/processed
cutoff_len: 2048
learning_rate: 2.0e-4
num_train_epochs: 1.0
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
lr_scheduler_type: cosine
warmup_ratio: 0.03
logging_steps: 10
save_steps: 100
output_dir: outputs/sft_qwen3_8b_lora
deepspeed: configs/llamafactory/deepspeed/zero2.json
