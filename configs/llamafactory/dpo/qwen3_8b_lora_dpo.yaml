# LLaMA-Factory DPO config placeholder for Stage 0.
# Assumes `llamafactory-cli` is installed separately.
stage: dpo
do_train: true
model_name_or_path: Qwen/Qwen3-8B
finetuning_type: lora
template: qwen
dataset: ecom_pref_seed
dataset_dir: data/processed
cutoff_len: 2048
learning_rate: 5.0e-5
num_train_epochs: 1.0
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
pref_beta: 0.1
logging_steps: 10
save_steps: 100
output_dir: outputs/dpo_qwen3_8b_lora
deepspeed: configs/llamafactory/deepspeed/zero2.json
